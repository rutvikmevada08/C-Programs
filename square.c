#include<stdio.h>
int main()
{
    int n;
    printf("Enter the number the number:");
    scanf("%d",&n);
    // printf("Enter the number the number 2:");
    // scanf("%d",&b);
   printf("The sqyare if num is :%d",n*n);
    return 0;
}